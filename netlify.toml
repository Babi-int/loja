# Site na Netlify: frontend React (pasta frontend/). API Node fica em outro host (Render, Railway, etc.).
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
